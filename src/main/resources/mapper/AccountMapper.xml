<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.accountbookserver.mapper.AccountMapper">

    <select id="selectAll" resultType="org.example.accountbookserver.entity.Account">
        SELECT * FROM t_record ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="org.example.accountbookserver.entity.Account">
        INSERT INTO t_record (user_id, type, amount, category, remark, create_time)
        VALUES (#{user_id}, #{type}, #{amount}, #{category}, #{remark}, CURRENT_TIMESTAMP)
    </insert>

    <update id="update" parameterType="org.example.accountbookserver.entity.Account">
        UPDATE t_record
        SET type=#{type}, amount=#{amount}, category=#{category}, remark=#{remark}
        WHERE id=#{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM t_record WHERE id=#{id}
    </delete>

</mapper>