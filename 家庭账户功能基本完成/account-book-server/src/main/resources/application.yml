server:
  port: 8081
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_account_book?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: yz669473
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.accountbookserver.entity
  configuration:
    map-underscore-to-camel-case: true